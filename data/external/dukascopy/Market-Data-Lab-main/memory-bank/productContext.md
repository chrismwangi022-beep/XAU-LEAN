# Product Context

Trading research needs reliable historical data with known provider, schema, timezone, coverage, and quality assumptions.

The current dataset supports bid/ask-aware backtesting for XAUUSD with one-minute Dukascopy data. Future datasets can be added without changing repository identity.

The first migration step keeps existing CSV paths intact so current backtest tooling remains compatible.

